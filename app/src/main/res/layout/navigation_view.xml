<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.NavigationView android:id="@+id/navigationView"
                                              xmlns:android="http://schemas.android.com/apk/res/android"
                                              xmlns:app="http://schemas.android.com/apk/res-auto"
                                              android:layout_width="wrap_content"
                                              android:layout_height="match_parent"
                                              android:layout_gravity="start"
                                              android:background="@drawable/img_navigation_background"
                                              app:headerLayout="@layout/navigation_view_header"
                                              app:itemIconTint="@color/navigation_menu_selector"
                                              app:itemTextColor="@color/navigation_menu_selector"
                                              app:menu="@menu/menu_navigation_view"/>

